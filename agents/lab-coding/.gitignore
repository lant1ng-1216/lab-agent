node_modules/
dist/
cli
cli-dev
openclaw/
docs/

# 本地密钥配置（避免误提交，均含 API Key）
freecode.env
lab-agent.env

# 各 agent 的独立配置目录
.freecode-config/
.lab-agent-config/

# 历史遗留：WorkBuddy 的记忆目录，Lab Code 不读取、不提交
.workbuddy/
